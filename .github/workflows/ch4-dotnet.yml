name: .NET

env:
 #ACR_USERNAME: ka123devopsreg
 #ACR_PASSWORD: BDGBwl8tw0dV56oNK28/7hIu3k13nvpl
 #ACR_LOGIN_SERVER: ka123devopsreg.azurecr.io
 registry: ka123devopsreg.azurecr.io
 repository: wth/dotnetcoreapp
 docker_path: Application/src/RazorPagesTestSample/
 tag: github.run_number
 
on:
  push:
    paths:
       - 'Application/*'
    branches: [ main ]
  #pull_request:
    #branches: [ main ]

jobs:
  build_and_test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    
    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      #with:
        #dotnet-version: 2.2
    - name: Restore dependencies
      run: dotnet restore Application/src/RazorPagesTestSample/*.csproj
    - name: Build
      run: dotnet build --configuration Release --no-restore Application/src/RazorPagesTestSample/*.csproj
    - name: Test
      run: dotnet test --no-build --verbosity normal  Application/tests/*
      
   
  #######################
  docker_build_and_push:
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    needs: build_and_test
    steps: 
    - uses: actions/checkout@v2
    - name: Docker login
      run: docker $registry -u ka123devopsreg -p pitxvDlI7XBs0R+aFLDDDDRiaG5sAhCL
      #uses: azure/docker-login@v1
      #with:
       #ACR name
       #username: ka123devopsreg #${{ secrets.ACR_USERNAME }}
       #ACR password
       #password: pitxvDlI7XBs0R+aFLDDDDRiaG5sAhCL #${{ secrets.ACR_PASSWORD }}
       #ACR URL
       #login-server: ka123devopsreg.azurecr.io #${{ secrets.ACR_LOGIN_SERVER }}
       
    - name: Docker build
      run: docker build -t $registry/$repository:${{github.run_number}} --build-arg build_version=${{github.run_number}} $docker_path
  
    - name: Docker Push
      run: docker push $registry/$repository:${{github.run_number}}  
